<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <!-- <link rel="icon" href="../../../../favicon.ico"> -->
    <title>Home page - Платежи</title>

    <% include ../common/bootstrap_css.ejs %>
    <% include ../common/font_awesome_css.ejs %>
    <link href="/navbar-top.css" rel="stylesheet">
</head>

<body>
    <nav class="navbar navbar-expand-md navbar-light bg-light mb-4">
        <a class="navbar-brand" href="#">Домофон-Сервис</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <% include ../common/full_menu.ejs %>
    </nav>

    <div class="container-fluid">
        <form class="form-inline">
            <a class="btn btn-primary btn-sm" href="/payments/add" id="add">
                <i class="fas fa-plus fa-fw" aria-hidden="true"></i>
            </a>
        </form>
        <p></p>
        <table class="table table-bordered table-hover table-sm">
            <thead class="thead-light">
                <tr>
                    <th scope="col" rowspan="2" style="width: 6%;" class="text-center align-middle">Дата</th>
                    <th scope="col" rowspan="2" style="width: 6%;" class="text-center align-middle">Номер<br> договора</th>
                    <th scope="col" rowspan="2" style="width: 10%;" class="text-center align-middle">Пролонгированный<br>договор</th>
                    <th scope="col" style="width: 6%;" class="text-center align-middle">Месяц</th>
                    <th scope="col" rowspan="2" style="width: 6%;" class="text-center align-middle">Льготник</th>
                    <th scope="col" style="width: 12%;" class="text-center align-middle">Улица</th>
                    <th scope="col" style="width: 12%;" class="text-center align-middle">Город</th>
                    <th scope="col" rowspan="2" style="width: 6%;" class="text-center align-middle">Квартира</th>
                    <th scope="col" rowspan="2" style="width: 6%;" class="text-center align-middle">Оплачено</th>
                    <th scope="col" rowspan="2" style="width: 9%;" class="text-center align-middle">Сумма</th>
                    <th scope="col" rowspan="2" style="width: 9%;" class="text-center align-middle">Способ<br> оплаты</th>
                    <th scope="col" rowspan="2" style="width: 12%;" class="text-center align-middle">Действия</th>
                </tr>
                <tr>
                    <th scope="col" class="text-center align-middle">Год</th>
                    <th scope="col" class="text-center align-middle">Дом</th>
                    <th scope="col" class="text-center align-middle">Подъезд</th>
                </tr>
            </thead>
            <tbody>
                <% for (var cnt = 0, max = data.length; cnt < max; cnt++) {%>
                    <tr>
                        <td rowspan="2" class="text-center align-middle">
                            <%= moment(data[cnt].create_date).format( 'DD.MM.YYYY') %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <%= data[cnt].contract_number %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <%= data[cnt].m_contract_number %>
                        </td>
                        <td class="text-center align-middle">
                            <%= data[cnt].pay_month %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <div class="checkbox checkbox-success"><input type="checkbox" class="styled" <%= +data[cnt].privilege == 1 ? ' checked' : '' %>><label></label></div>
                        </td>
                        <td class="text-left">
                            <%= data[cnt].street_name %>
                        </td>
                        <td class="text-left">
                            <%= data[cnt].city_name %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <%= data[cnt].number + '' + utils.decodeApartmentLetter( data[cnt].letter ) %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <%= moment(data[cnt].pay_date).format( 'DD.MM.YYYY') %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <%= data[cnt].amount.toFixed(2) %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <%= data[cnt].org_name %>
                        </td>
                        <td rowspan="2" class="text-center align-middle">
                            <a class="btn btn-primary btn-sm" href="/payments/edit/<%= data[cnt].id %>" id="edit">
                                <i class="far fa-edit fa-fw" aria-hidden="true"></i>
                            </a>
                            <a class="btn btn-danger btn-sm" href="#" onclick="confirmDelete(+<%= data[cnt].id %>);">
                                <i class="far fa-trash-alt fa-fw" aria-hidden="true"></i>
                            </a>
                        </td>
                    </tr>
                    <tr>
                        <td class="text-center">
                            <%= data[cnt].pay_year %>
                        </td>
                        <td class="text-right">
                            <%= data[cnt].house_number %>
                        </td>
                        <td class="text-right">
                            <%= data[cnt].porch %>
                        </td>
                    </tr>
                <%}%>
            </tbody>
        </table>
        <% if (pageCount > 0) { %>
            <nav aria-label="...">
            <ul class="pagination text-center table_pagination">
                <% if (currentPage == 1) { %>
                    <li class="page-item disabled">
                        <span class="page-link">&laquo;</span>
                    </li>
                    <% } else { %>
                        <li class="page-item">
                            <a class="page-link" href="/payments">&laquo;
                        </a>
                    </li>
                <% } %>
                <% var i = (Number(currentPage) > 5 ? Number(currentPage) - 4 : 1) %>
                <% if (i !== 1) { %>
                    <li class="page-item disabled">
                        <a class="page-link">...</a>
                    </li>
                <% } %>
                <% for (; i <= (Number(currentPage) + 4) && i <= pageCount; i++) { %>
                    <% if (i == currentPage) { %>
                        <li class="page-item active">
                            <span class="page-link">
                                <%= i %>
                                <span class="sr-only">(current)</span>
                            </span>
                        </li>
                    <% } else { %>
                        <li class="page-item">
                            <a class="page-link" href="/payments/<%= (i == 1) ? '' : (i - 1) * Number(visibleRows) %>">
                                <%= i %>
                            </a>
                        </li>
                    <% } %>
                    <% if (i == Number(currentPage) + 4 && i < pageCount) { %>
                        <li class="page-item disabled">
                            <a class="page-link">...</a>
                        </li>
                    <% } %>
                <% } %>
                <% if (currentPage == pageCount) { %>
                    <li class="page-item disabled">
                        <a class="page-link">&raquo;</a>
                    </li>
                <% } else { %>
                    <li class="page-item">
                        <a class="page-link" href="/payments/<%= (pageCount - 1) * Number(visibleRows) %>">&raquo;</a>
                    </li>
                <% } %>
             </ul>
             </nav>
          <% } %>
    </div>

    <div class="modal" id="modalDeleteRecord" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Подтверждение</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <form>
                        <input type="text" hidden id="deletedUID" name="deletedUID" value="">
                    </form>
                    <p>Удалить запись?</p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-warning" id="deleteRecord">Удалить</button>
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Отмена</button>
                </div>
            </div>
        </div>
    </div>

    <script src="/bower_components/jquery/dist/jquery.min.js"></script>
    <script src="/bower_components/bootstrap/assets/js/vendor/popper.min.js"></script>
    <script src="/bower_components/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src="/bower_components/bootstrap/assets/js/vendor/holder.min.js"></script>
    <script>
        function confirmDelete(id) {
            $('#deletedUID').val(id);
            $('#modalDeleteRecord').modal({'keyboard': true});
        }

        $(document).ready(function () {
            $('#deleteRecord').click(function() {
                $('#modalDeleteRecord').modal('hide');
                $.ajax({
                    type: 'POST',
                    url: '/payments/delete',
                    dataType: 'json',
                    async: true,
                    data: {
                        'id': $('#deletedUID').val()
                    },
                    success: function (data) {
                        var path = window.location.origin + '/payments';
                        window.location.href = path;
                        return;
                    },
                    error: function (data) {
                        alert(data);
                    }
                });
                
            });
        });
    </script>
</body>

</html>